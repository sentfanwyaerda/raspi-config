#!/bin/sh
file="/var/zero.txt"

if [[ -n "$1" ]] ; then
	time dd if=/dev/zero of=$file bs=1 count=0 seek=$1
else
	time dd if=/dev/zero of=$file
fi

chmod 0777 $file
chown www-data $file
chgrp www-data $file

if [[ ! -n "$1" ]] ; then
	halt
fi
